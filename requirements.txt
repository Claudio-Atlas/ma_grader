# MA Grader engine runtime dependencies.
# Pin these before building the PyInstaller sidecar so the frozen binary
# matches what you test with.
openpyxl==3.1.5
requests==2.32.3
urllib3==2.2.3
python-docx==1.1.2
pypdf==5.1.0

# Build-time only (not imported by the engine; used to freeze the sidecar):
# pyinstaller==6.11.1
