#!/bin/bash
# DEPRECATED. The app no longer bundles a Python venv.
# The engine is now frozen into a self-contained binary by PyInstaller.
# Use:  npm run freeze:engine   (see engine.spec)
echo "stage-engine.sh is deprecated — use 'npm run freeze:engine' instead." >&2
exit 1
